# Feedback hooks

Python implementation: `juntai.documentation.feedback`. Exposes privacy-safe,
advisory hooks and injected evidence dereference; feedback cannot mutate,
publish, approve, or roll back content.
