# Framework-neutral loader

Python implementation: `juntai.documentation.loader`. Verifies exact immutable
pins, producer build identity, and OpenAPI/MCP descriptor digests without
owning a runtime registry or domain behavior.
