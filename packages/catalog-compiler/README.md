# Catalog compiler

Python implementation: `juntai.documentation.catalog`. Compiles and validates a
signed immutable static index and performs pure, fail-closed pin selection.
Key lookup and signing remain injected responsibilities.
