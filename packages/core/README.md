# Core

Python implementation: `juntai.documentation.canonical`, `descriptor`,
`packager`, and `schema_validation`. Owns only generic schemas, validation,
locking, canonicalization, and deterministic packaging.
