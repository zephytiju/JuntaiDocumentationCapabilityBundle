# Invalid fixtures

Negative conformance cases are derived from the exact valid lock in tests so a
single mutation isolates each failure: unknown Tool, changed signature, schema
drift, graph cycle, invalid example and prohibited credential.
